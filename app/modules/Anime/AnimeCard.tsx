import React from "react";

type AnimeCardProps = {
  orientation: "vertical" | "horizontal";
};
const AnimeCard = ({orientation}: AnimeCardProps) => {
  return <div>AnimeCard </div>;
};

export default AnimeCard;
